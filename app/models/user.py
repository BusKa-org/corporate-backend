"""User, Motorista, Gestor — the three roles every client of this stack has.

No `Aluno` here: PaqTcPB's rider is `Passageiro` (see passageiro.py), with no
guardian and no school link. That is the whole reason this file starts empty
instead of copying municipal-backend's `models/user.py` — see
docs/adr/0001-produto-do-zero-em-vez-de-fork.md.
"""
